---
version: '3.8'
services:
  postgres:
    container_name: tealacarte
    image: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tealacarte}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-smoke"}
    ports:
      - "5432:5432"
    restart:
      unless-stopped
